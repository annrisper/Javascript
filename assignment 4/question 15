function objectToPairs(obj) {
  const pairs = [];
  for (let key in obj) {
    pairs.push([key, obj[key]]);
  }
  return pairs;
}

// Example usage:
const myObject = {
  property1: 'value1',
  property2: 'value2'
};

const pairs = objectToPairs(myObject);
console.log(pairs);